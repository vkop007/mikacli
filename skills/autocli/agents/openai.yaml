interface:
  display_name: "AutoCLI"
  short_description: "Use AutoCLI providers correctly"
  default_prompt: "Use $autocli to choose the right AutoCLI category and provider, prefer --json for agent work, and recover cleanly from auth or session issues."

policy:
  allow_implicit_invocation: true
